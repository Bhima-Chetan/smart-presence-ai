# Ultra-minimal requirements for Render deployment
# Completely avoids dlib and face_recognition compilation issues

# Core web framework
Flask==2.3.3
gunicorn==21.2.0
Werkzeug==2.3.7

# Data processing
numpy==1.24.3
pandas==2.0.3

# Image handling (basic)
Pillow==10.0.0
opencv-python-headless==4.8.1.78

# Utilities
python-dateutil==2.8.2

# Optional: These may work but aren't required
# requests>=2.25.0
