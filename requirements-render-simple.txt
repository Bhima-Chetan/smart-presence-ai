# Ultra-minimal requirements for Render deployment
# Fixed NumPy version compatibility with OpenCV

# Core web framework
Flask==2.3.3
gunicorn==21.2.0
Werkzeug==2.3.7

# Data processing - CRITICAL: NumPy must be < 2.0 for OpenCV compatibility
numpy==1.24.3
pandas==2.0.3

# Image handling - OpenCV compatible with NumPy 1.x
opencv-python-headless==4.8.1.78
Pillow==10.0.0

# Utilities
python-dateutil==2.8.2
